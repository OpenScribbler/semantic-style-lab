---
title: Shareable audit fixture
---

# Configure the client

Set up the command-line client in real time.

The credential is rejected; therefore, the client requests a new credential.

Use `const ready = true;` in the example.
