# Contextual vocabulary sample

Set up the client, and then complete the set up.

Install the command line client from the command-line.

The dashboard provides real time updates and refreshes in real-time.

Select **Setup** to continue. Run the `setup` command if the button is unavailable.
