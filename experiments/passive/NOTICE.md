# Quoted style guide text

`rules.json` and the rubrics under `microsoft/` and `redhat/` quote short passages
from the Microsoft Writing Style Guide, the Red Hat supplementary style guide, and
IBM Style, with a source URL for each. The quotes are here so readers can check
each rubric against the rule it encodes. The quoted text belongs to its
publishers and isn't covered by this repository's MIT license.
